# BBC Reader - 実装済み機能

## 画面一覧

### 記事一覧（`/`）
- localStorageから記事を取得して一覧表示
- 各記事に既読・未読バッジを表示
- 記事タイトルをクリックで詳細ページへ遷移
- 記事の削除ボタン
- 記事追加ページへのリンク

### 記事追加（`/add`）
- タイトル・URL・スクリプトを入力して保存
- スクリプトは `splitSentences()` でセンテンスに分割して保存
- 保存後は一覧ページへリダイレクト

### 記事詳細（`/article/[id]`）
- タイトル・元記事リンクを表示
- センテンス一覧を表示
- 各センテンスにメモ入力欄（入力のたびにlocalStorageへ自動保存）
- 既読・未読の切り替えボタン
- 記事が存在しない場合は3秒後に一覧へリダイレクト

## データ

- 保存先：localStorage（キー: `bbc-reader-articles`）
- DBなし・認証なし
